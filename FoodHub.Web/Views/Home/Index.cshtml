@model IEnumerable<FoodHub.Data.Models.Restaurant>

@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>ASP.NET</h1>
    <div>Hello World 😍!</div>
    <div>@DateTime.Now.Year</div>
    @*<div>
            There are @Enumerable.Count(Model) restaurants in the db.
        </div>*@
    <div>
        There are @Model.Count() restaurants in the db.
    </div>

</div>

@*<ul>
    @foreach (var restaurant in Model)
    {
        <li>@restaurant.Name</li>
    }
</ul>*@
